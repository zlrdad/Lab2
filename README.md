# Scaling & Retaining Canada's Champions

An interactive research report on Ontario's capital markets, growth-company financing gaps, and policy options — why Canada builds world-class companies but struggles to scale and retain them, and what to do about it. **11 numbered chapters, an unnumbered grounding Profile page, an unnumbered Atlas of the 19 comparator jurisdictions, 2 interactive data tools, and 19 company case studies** (16 domestic — the 15 highlighted roster champions plus the Nortel cautionary precedent — and 3 international comparators), covering the financing gap, the shrinking public markets, what other jurisdictions have done, and a costed set of policy recommendations.

## How to view

- **Live site:** [zlrdad.github.io/Lab2](https://zlrdad.github.io/Lab2/) *(goes live once GitHub Pages is enabled — see Maintainers' notes)*
- **Locally:** open `index.html` in any modern browser and use the top nav. No build step, no server required — the whole report (including both interactive dashboards) works from `file://`. It runs with JavaScript disabled too: every figure, chart and table is legible statically; JS only adds animation and interactivity on top. Zero external scripts are loaded (the one exception is a Google Fonts stylesheet).

## Data & attribution

Statistics from the **World Federation of Exchanges (WFE)** are used throughout the exchanges chapters and the Global Exchanges Explorer, credited inline where required (look for the "Source: WFE" marks). Other data draws on **TMX**, **Bloomberg**, the **OECD**, **Statistics Canada**, the **IMF**, and additional government, academic and institutional sources — each figure carries a source pill (tap or click it) linking to its full provenance and confidence grade, with the complete source register on the [Methodology](methodology.html#register) page.

## Disclaimer

This is a research report prepared for policy analysis. It is not investment advice, legal advice, or an offer of any kind. Figures are point-in-time and are labelled with their as-at date; private-company valuations are last-priced or press-reported. See the Methodology page for data vintages and confidence grades.

© 2026 — research report. _[Author name to be added]_

---

## Maintainers' notes

The notes below document the report's design system and build conventions for anyone editing or extending the site. They are not required reading to view the report.

### The design system

The report is built in the **Mission Control** design system (Deep-Indigo base × Azure master accent, monochrome-plus-one-accent, open composition — card-less, text/numerals/inline-SVG charts sit directly on the background). The single source of truth is the vault page **`Planning/Report Design System — Mission Control.md`** and its reference implementation **`Dashboards/Report Flagship Demo v3 — Open Composition.html`**. `styles.css` and `app.js` in this folder extract that system so every page shares one token set, one component library and one behaviour script. A **Poster OSC** light register lives under `html[data-theme="light"]`; the top-bar toggle switches it (in-page only, no `localStorage`).

### Per-page accent assignment (one colour per page, §4)

Declare on `<body>`: `<body data-accent="…">`. One hook re-points the master accent for the whole page.

| data-accent | Colour | Page zone | Pages |
|---|---|---|---|
| `azure`  | Azure `#4da3ff`         | **Report spine** (master) | `index`, `canada-ontario`, `growth-prize`, `strengths`, `public-markets`, `recommendations`, `methodology`, `exchanges` |
| `violet` | Electric Violet `#7c83ff` | Comparators / international | `what-works`, `comparators`, `global-exchanges`, `atlas` |
| `cyan`   | Ice Cyan `#6fd3ff`       | Sectors / champions        | `growth-sectors`, `champions` |
| `orange` | Signal Orange `#ffb454`  | Value leakage / risk       | `financing-gap` |

Added July 12 2026 (Session 2): `canada-ontario` takes **azure** — it's a report-spine grounding page (the scale figures the rest of the arithmetic runs on), not a comparator page. `atlas` takes **violet** — it's the international/comparator family (19 jurisdictions, the same family as `what-works`/`comparators`/`global-exchanges`).

Signal Orange is also the reserved **leakage-strip** zone within any page: wrap a section in `id="leak"` (or `class="leak-zone"`) and it claims orange locally regardless of the page accent (see the growth-prize page's "What's at stake" strip).

### Shell conventions for page authors

- **Nav include pattern (16 links, site-wide mobile nav added July 12 2026 — Session 2).** There is no server-side include; the topbar block is copied into each page's `<body>` (relative hrefs so it works from `file://`). The **brand is the home affordance** — `<a class="tb-l brand" href="index.html">…Canada's Champions…</a>`, identical and shortened on every page. The desktop crumb row (`<span class="links">`) is now **16 links in three visually grouped clusters**: the unnumbered **Profile** pointer (`canada-ontario.html`) first, then the **11 report-narrative links in canonical arc order** — Overview · Profile · Prize · Strengths · Sectors · Markets · Gap · Abroad · Compare · Champions · Recs · Method — a thin vertical divider (`<span class="tb-div">`), the mono "Data" group label (`<span class="tb-grouplab">`), then **3** interactive/reference links — Exchanges · Global · **Atlas** (added this session). Labels are **slim** (abbreviated for width; the abbreviated ones carry a `title` attribute with the full name). Mark whichever link matches the current page with `class="on" aria-current="page"`. All 15 top-level pages ship the identical desktop block; only `.on`/`aria-current` differs.
  ```html
  <span class="links">
    <a href="index.html">Overview</a>
    <a href="canada-ontario.html" title="Canada &amp; Ontario — Economic &amp; Investor Profile">Profile</a>
    <a href="growth-prize.html">Prize</a>
    <a href="strengths.html">Strengths</a>
    <a href="growth-sectors.html">Sectors</a>
    <a href="public-markets.html" title="Public Markets">Markets</a>
    <a href="financing-gap.html" title="The Financing Gap">Gap</a>
    <a href="what-works.html" title="What Works Abroad">Abroad</a>
    <a href="comparators.html" title="The Comparators">Compare</a>
    <a href="champions.html">Champions</a>
    <a href="recommendations.html" title="Recommendations">Recs</a>
    <a href="methodology.html" title="Methodology &amp; Sources">Method</a>
    <span class="tb-div" aria-hidden="true"></span>
    <span class="tb-grouplab">Data</span>
    <a href="exchanges.html" title="The Exchanges — interactive">Exchanges</a>
    <a href="global-exchanges.html" title="Global Exchanges Explorer">Global</a>
    <a href="atlas.html" title="The Atlas — jurisdictions map">Atlas</a>
  </span>
  ```

- **Mobile nav pattern (breakpoint 940px, added July 12 2026 — the site-wide fix for tabs being invisible on mobile).** Root cause of the bug: `styles.css` simply hid `.links` below 760px with no replacement affordance, so mobile visitors only had the two bottom-of-page `.nextlist` buttons to navigate with — no way to jump chapters. The fix is a **sticky slim bar + a `<details>`-based full-screen sheet**, all inside one `@media(max-width:939.98px)` block (desktop is untouched above that width — the horizontal `.links` show and the two mobile-only elements are `display:none`, adding nothing to desktop rendering):
  - **Slim bar** — below 940px the topbar collapses to: wordmark (`.tb-l.brand`) · a centred current-page label (`.tb-here`, mono, ellipsis-truncated, filled in by `app.js`) · the theme toggle · a pill-shaped **Menu** trigger (`.navtrigger`, 44px tap target, hamburger icon that swaps to an × when open).
  - **Sheet** — a native `<details class="navsheet" id="navSheet">` / `<summary class="navtrigger">` disclosure wrapping a full-screen `<nav class="sheet" id="navSheetPanel">`. Because it's native `<details>`, **it opens and closes with JavaScript entirely off** (the static-first contract) — `app.js` only *enhances* it: scroll-lock (`body.nav-open{overflow:hidden}`) while open, `aria-expanded` mirroring, close-on-link-tap / Esc / outside-click, and focus return to the trigger on close. The sheet groups links under `.sheet-lab` headers — **"Chapters"** (Overview through Methodology, Profile included with an unnumbered `◇` glyph instead of a chapter number) then **"Data"** (Exchanges · Global · Atlas, all `·` glyphs) — and marks the current page with `.on` (accent-tinted row + a glowing dot), matching the desktop `.on` state.
  - **Case pages** (`cases/*.html`) keep their own **compact two-link desktop topbar** (Overview + The Champions, no `.on`) — this is deliberate, not a nav regression (confirmed in the fresh-eyes review, `research/review-new-pages.md` flag #2) — but they carry the **full mobile sheet**, including an `.allcases` "‹ All case studies" pointer row above "Chapters", and Profile + Atlas are present in that sheet like every other page.
  - Chrome uses the page's master-accent tokens (`var(--accent…)`), so the sheet auto-matches whichever accent the page declares — no page-specific nav styling needed.
  - **To add a new nav item now:** update the desktop `<span class="links">` block (one more `<a>`, plus the Data-group divider placement if it's a tool/reference link) **and** the mobile `<nav class="sheet">` block (one more `<a>` under the right `.sheet-lab` group, with an `<span class="sn">` number/glyph + `<span class="st">` label) **on every one of the 15 top-level pages** (16 including the case-page mobile sheet, which is a 16th copy per case file — currently 19 case files). There is no shared include, so this is a mechanical multi-file edit — grep for `class="links"` and `id="navSheetPanel"` across `*.html` and `cases/*.html` to find every copy. Keep the two blocks (desktop crumbs, mobile sheet) in the same order.
- **The report arc (canonical reading order).** Overview → **The Prize** (`growth-prize`) → **The Strengths** (`strengths`) → **The Sectors** (`growth-sectors`) → **Public Markets** (`public-markets`) → **The Financing Gap** (`financing-gap`) → **What Works Abroad** (`what-works`) → **The Comparators** (`comparators`) → **The Champions** (`champions` + `cases/`) → **The Recommendations** (`recommendations`) → **Methodology**. Every page's outro (`.nextlist`) hands **primarily to the next chapter** in this order (with 1–2 secondary/back links); the Overview tiles list the full chapter set + the two data tools. `exchanges` / `global-exchanges` are data tools, reachable from the nav "Data" group and the Overview tiles.
- **Source surface (QC pass July 8 2026 — sources off the page).** The **source pill + drawer is THE source surface**, backed by the full static register on `methodology.html#register`. No page carries visible `.srcline` one-liners, footer source lists, or source-naming captions any more. Each `.provpill` is a real `<a href="methodology.html#register">` (`../methodology.html#register` from `cases/`) that JS upgrades into the drawer; with JS off it jumps to the register (the canonical no-JS target). Footers are `.ft-h` + one `.ft-src` pointer line + `.footid`. **Sole exception:** a WFE-derived chart keeps one minimal visible `Source: WFE` credit (contractual) — `public-markets` velocity, `growth-prize` IPO chart, and `global-exchanges` (`.wfeattr`). `methodology.html` keeps its full `.srcreg` register (with `id="register"`); the rich per-figure provenance lives in each page's `window.PROV` drawer data.
- **Declare `data-accent`** on `<body>` per the table above (default is azure if omitted).
- **Provenance (`window.PROV`).** Each page that uses source pills defines, in a `<script>` **before** `app.js`, a `window.PROV` object mapping `data-prov` key → `{ t:"title", meta:"HTML", conf:"high|medium|soft" }`. Every `.provpill` is authored as a real `<a href="#sources">…</a>` so it degrades to a jump to the footer Sources block with JS off; `app.js` upgrades it into a drawer trigger only when a `PROV` entry exists. Include the shared drawer markup (`#scrim`, `#drawer` with `#drawerX` + `#drawerBody`) once per page, before the PROV/app.js scripts.
- **Inline-SVG chart rule (§5).** Every chart is **build-time inline SVG** with pre-computed coordinates and **every colour a `var(--…)` token** (never a literal hex in the SVG) — that is what lets the theme/accent switchers reskin charts automatically. No runtime chart library. Wrap each SVG in `.svgwrap` with `role="img"` + a prose `aria-label` stating the headline numbers.
- **Chart-colour policy (spec §5.5a, v2.8 — added July 7 2026).** One-accent-plus-flat-grey charts read too bland, so: the **subject** series gets a **gradient** (subject bar = a horizontal `<linearGradient>` `var(--accent-deep)`→`var(--accent)`; area-under-a-line = a vertical `var(--accent)` .30→0 fade) with the `.subj-glow` shadow; **ordered ranked bars** use the `.ramp-1…6` accent-intensity classes in rank order (not flat grey); **named comparators** the reader must read use the family data palette (`--pg-spine/comp/sector/leak`, `--sx-*`, semantic tokens) with dash patterns retained; grey (`--steel`/`--dim`) is reserved for genuine de-emphasised backdrop only. Every gradient stop is a `var()` token so it rethemes on the base/accent/Poster-OSC switch; give each gradient a **unique per-chart id**. The two JS dashboards inject their subject gradient into `<defs>` at build time and ramp non-subject bars via a stepped-opacity accent fill.
- **Zero-external-scripts rule.** No external `<script>` tags. The only permitted external asset is the Google Fonts `<link>`. (Chart.js is permitted **only** for a genuinely-interactive, verified-rendering widget per spec §5.2 — not the default; prefer inline SVG.)
- **Static-first contract.** Every figure, table and narrative line must be legible with JavaScript disabled and open cleanly from `file://`. All `app.js` features are enhancement-only, each wrapped in its own `try/catch`; `prefers-reduced-motion` is respected; no `localStorage`.
- **Count-ups.** Give a numeral `data-count` (+ optional `data-dec`, `data-prefix`, `data-suffix`) and it animates on reveal; put the final value as the element's text so it reads with JS off. `#issuerNum` / `#leakNum` have dedicated runners. (The old hardcoded `#sigNum` C$210B runner was removed July 7 2026 — no page uses that id; the growth-prize hero numeral is a generic `.fig-n[data-count]`.)
- **Shared list/label components (promoted July 6–7, 2026 QA pass).** Two component families that recurred across pages have been promoted into `styles.css` proper (chart-annotation / list-components area) — page authors should use these directly rather than re-declaring them in a scoped `<style>`:
  - **`.hbar-lab` / `.hbar-val`** (+ `.subj` modifier) — the label/value text classes for inline-SVG ranked-bar charts. One definition serves every page; `.subj` marks the bar the page argues from.
  - **`.evlist` / `.evrow`** (+ optional `.evrow .es` sub-label and `.evrow .el` italic "lesson" line) — the two-column hairline evidence row. The shared rule sets a sensible default `grid-template-columns`/`gap`; a page may override just those two properties in its own scoped block if its label column genuinely needs a different width (that's a tuned instance, not a duplicate — don't re-declare the whole selector).
  - **`.ramp-1…6` / `.rampL-1…4` / `.subj-glow` / `.subj-glow-strong`** (added July 7 2026, chart-colour policy v2.8) — the accent intensity-ramp classes for ranked-bar comparators (`.ramp-*` = page accent at stepped opacity; `.rampL-*` = Signal-Orange for leak zones, with light-register opacity bumps so paper bars stay legible) and the subject-bar drop-shadow glows. Use these on `<rect>`s directly; the gradient `<linearGradient>` defs themselves live per-SVG (SVG requires the def in-document) but their stops are `var()` tokens — see the convention block at the top of the chart-utilities section in `styles.css`.
  - Only promote a component when **identical rules appear on 3+ pages**; page-specific components (`.board3`, `.roster`/`.costudy`, `.cmpgrid`, `.lvrlist`/`.xwlist`, `.laylist`/`.grdlist`/`.cvlist`, etc.) stay scoped where they live.

### Pages

1. **Overview** (`index.html`) — the cover + tiles to every chapter and data tool
◇ **Profile** (`canada-ontario.html`, added July 12 2026 — Session 2) — "The Ground Truth," the report's grounding/scale page, deliberately unnumbered (before chapter 01, `◇` glyph in the nav sheet like Overview). Answers "how big is this place, who invests here, what's already working, what numbers does the whole argument rest on" before the report pivots to the gap/strengths/diagnosis chapters. See "Ownership rulings" below for what it owns outright vs. what it points to.
2. **The Growth Prize** (`growth-prize.html`) — the opening content chapter: the GDP-per-capita slide (71.4% of the US), the headline gap, the sticky IPO scene, the VC/GDP bullet, the leakage strip, the Leaders Fund founder-retention beat (§06b — 32.4% of 2024-cohort high-potential startups HQ'd in Canada, down from 67%+ in 2015–19) and a household stock/flow mininote tying the housing-vs-IP investment ratio to real-estate-heavy household wealth
3. **The Strengths** (`strengths.html`) — what Ontario has to work with: talent, clean low-cost power, the university base, the Maple-8 capital pool, and a second cluster (band 06 — Ottawa & Kanata North)
4. **The Sectors** (`growth-sectors.html`) — nine growth sectors through the OEA lens; the housing-vs-IP investment mix (2.76×) + an interactive per-sector explorer + a public-market-presence module (§05 — TSX/TSXV issuer count & market cap by sector, a board-toggle top-10-issuer table per sector with interlisting badges, and the true-IPO-vs-ETP-creation tally)
5. **Public Markets** (`public-markets.html`) — the three-board divergence (TSX / TSXV / CSE), turnover velocity, the IPO drought (now with the take-private off-ramp alongside it), the missing middle, the US exit ramp (§06 — interlisting/MJDS mechanics, the Doidge-Karolyi-Stulz cross-listing premium, the Xanadu SPAC contrast) and who owns the market (§07 — direct vs. look-through household equity ownership)
6. **The Financing Gap** (`financing-gap.html`) — VC/GDP gap (now paired with a second, broader 15-jurisdiction Compendium-basis chart, §01b), the scale-up stage, domestic-vs-foreign capital, the founder-formation decline (§02 — the BDC Index) and the capital sitting idle next door (§06 — unused TFSA room)
7. **What Works Abroad** (`what-works.html`) — comparator tile-grid + the program toolkit + the four comparator-highlight charts (§04b), which tease →
8. **The Comparators** (`comparators.html`) — the full interactive explorer: Canada vs 14 economies across seven dimensions
9. **The Champions** (`champions.html` + `cases/`) — a 15-company highlighted roster by outcome (fully linked, 15/15 + Nortel = 16/16) with 19 click-through case pages (16 domestic incl. Nortel, 3 international) + the four patterns
10. **The Recommendations** (`recommendations.html`) — the four levers
11. **Methodology & Sources** (`methodology.html`) — data layers (now eight, with a new household/formation StatCan-and-CRA layer), confidence grades, caveats (incl. the VC/GDP dual-basis, velocity-basis, interlisting, sector-cut-partiality, formation-no-comparator and WFE-all-in rulings) + the full static source register (`#register`)

Plus two interactive dashboard pages, reachable from the nav's "Data" group and from two extra tiles on the Overview page:

12. **The Exchanges** (`exchanges.html`) — an interactive TSX / TSXV / CSE board switcher: live KPI row + sector-distribution chart per board, plus a TSX operating-vs-investment-products toggle that exposes the "71% of the senior board is funds" story, and an interlisting module (§05 — the venue-by-venue and sector-by-sector cut of the 30.7% interlisting rate, plus the TSXV→TSX graduation pipeline). Baked dashboard data in `window.EXDATA`; the build-time SVG renders the TSX-operating view with JS off.
13. **Global Exchanges Explorer** (`global-exchanges.html`) — an interactive World Federation of Exchanges comparator: pick an indicator (IPO listings, market cap, value traded, etc.) and toggle up to six of 25 exchanges on a shared line chart, 2010–2025, with an on-page basis note clarifying that WFE's all-in listed-company count (TMX 3,701) reconciles with the vault's MiG all-in tally (3,744) — not the 672 operating-issuer figure used elsewhere on the site. Baked dashboard data in `window.WFE`; the build-time SVG renders the "IPO drought" preset (TMX vs Korea, Hong Kong, Nasdaq-US) with JS off.
14. **The Atlas** (`atlas.html`, added July 12 2026 — Session 2) — "Where the playbook comes from": an inline-SVG world map (+ a Europe inset solving the historical crowding problem) with 19 click-through jurisdiction packages — Canada as subject-at-a-glance plus 14 core and 4 expansion (†) comparators — each with per-jurisdiction metrics and typed initiatives across five categories (public markets / private capital / growth companies / scaling & retention / innovation). See "The Atlas data pipeline" below for how it's built and regenerated.

### Page count

**15 top-level pages** (the 11 numbered chapters + Overview + the two Session-2 additions, Profile and Atlas, both deliberately unnumbered + the two interactive dashboards) plus **19 `cases/` pages** = **34 HTML files** on the shared shell (`Champions-Report.html`, the superseded legacy single-file, is not counted).

### Migration status

All **15 top-level pages** (plus the 19 `cases/` pages) are on the shared Mission Control shell (`styles.css` + `app.js`). The July 8 2026 **holistic coherence pass** wired the three later-built pages (`strengths`, `growth-sectors`, `comparators`) into the shared nav, enforced the report arc through every outro, moved all sources off the page into the pill drawers + the Methodology register, re-tallied the champions roster to 15 (adding Waabi, 1Password, Clearpath + a Nortel cautionary link, which also made those four case pages reachable from the hub), and reconciled cross-page figures. The legacy compatibility block was removed from `styles.css` in the July 6–7 2026 QA pass. The overnight **July 12 2026 build** (Session 1) added new sections to six pages, built the last 5 case pages (Wealthsimple, StackAdapt, Clio, Kinaxis, MDA Space — the roster is now fully linked, 15/15 + Nortel = 16/16), and ran a full page-by-page vault-scan + cross-page cohesion pass; no page count or accent changed that session. **Session 2 (July 12 2026, daytime)** added the two new pages (`atlas.html`, `canada-ontario.html`) and rebuilt the nav site-wide for mobile — see "Content additions (Session 2)" below.

| Page | Shell | Accent | Notes |
|---|---|---|---|
| `index.html` | ✅ migrated | azure | Re-skinned exemplar; `.navgrid` gained a Profile tile (◇ · The Ground Truth) + an Atlas data tile in Session 2 |
| `canada-ontario.html` | ✅ built (Session 2, Jul 12 2026) | azure | Unnumbered Profile page — see "Ownership rulings" below |
| `growth-prize.html` | ✅ migrated | azure | Ported from the v3 flagship demo; the second exemplar |
| `strengths.html` | ✅ coherence pass (Jul 8) | azure | Chapter 2 — the endowment; wired into the shared nav + arc outro |
| `growth-sectors.html` | ✅ coherence pass (Jul 8) | cyan | Chapter 3 — OEA interactive per-sector explorer; `#secSrc` source element hidden (sources off page) |
| `comparators.html` | ✅ coherence pass (Jul 8) | violet | Chapter 7 — interactive 14-economy explorer; what-works §04b now teases → here |
| `public-markets.html` | ✅ migrated (Jul 6, 2026) | azure | Chart.js replaced with inline SVG; `.board3` board-comparison component |
| `financing-gap.html` | ✅ migrated (Jul 6, 2026) | orange | Chart.js replaced with inline SVG; introduced the `.evlist`/`.evrow` evidence-row pattern (now shared, see below) |
| `champions.html` | ✅ migrated (Jul 6, 2026) | cyan | `.roster`/`.costudy` company rows carry the reserved stayed/left/acquired/failed semantic code |
| `what-works.html` | ✅ migrated (Jul 6, 2026) | violet | D3/topojson map replaced with a build-time inline-SVG comparator tile grid (`.cmpgrid`) — zero external scripts |
| `recommendations.html` | ✅ migrated (Jul 6, 2026) | azure | `.lvrlist`/`.xwlist` hairline rows replace the old nested card grids |
| `methodology.html` | ✅ migrated (Jul 6, 2026) | azure | `.laylist`/`.grdlist`/`.cvlist` hairline rows + the full static `.srcreg` source register |
| `exchanges.html` | ✅ migrated (Jul 7, 2026 QA pass) | azure | New interactive dashboard; `.bswitch`/`.bchip`/`.kpirow` page-scoped components; wired into the shared nav |
| `global-exchanges.html` | ✅ migrated (Jul 7, 2026 QA pass) | violet | New interactive dashboard; `.picker`/`.indbtn`/`.exchip`/`.serieskey` page-scoped components; wired into the shared nav |
| `atlas.html` | ✅ built (Session 2, Jul 12 2026) | violet | Map + 19 jurisdiction click-throughs — see "The Atlas data pipeline" below |

All 15 pass the cross-page QA gate: zero external `<script>` tags (Google Fonts `<link>` only), a `data-accent` declared on every `<body>`, the identical 16-link grouped desktop topbar (+ mobile sheet) with the correct `.on`/`aria-current` per page, a `window.PROV` ↔ `data-prov` bijection per page, every SVG/scoped-style colour resolved through a `var(--…)` token (no literal hex), and every `.provpill` degrading (JS off) to the `methodology.html#register` register. **Known open item, deliberately left:** the two `.tag-new` "NEW — for review" chips (the whole `comparators.html` page + the `what-works.html` §04b section) remain until Carlo verdicts them.

### Content additions (overnight July 12 2026)

Carlo's report-review notes were executed as a build wave (per-page sections), a page-by-page vault-scan pass (13 agents, one per page, each restrained to "strengthen existing beats first, at most one small new beat"), and a 3-auditor + 1-fixer cohesion pass. Full detail lives in the session's `progress.md`/`research/` working files (not part of the vault); this section records what actually landed on the pages.

**New sections, by page:**
- `growth-sectors.html` — new **§05 "Public-market presence"**: a TSX/TSXV toggle bar chart across 11 TMX sector categories (5 mapped 1:1 to growth sectors, cyan-ramped; the rest steel, marked †proxy), a top-10-issuer table with a sector-picker × board toggle (interlisting badges, top-issuer-share caption), and a static true-IPO block (6 TSX + 4 TSXV genuine operating-company IPOs in 24 months, vs. 200+ ETF/product creations in the same window). Stale Feb-2026 figures re-based to the May 31 2026 canonical cut throughout.
- `public-markets.html` — new **§06 "The US exit ramp"** (206/672 = 30.7% of operating TSX issuers interlisted; ~85% land in the US; MJDS mechanics, SOX-404 compliance cost, the Doidge-Karolyi-Stulz cross-listing premium with the King & Segal Canadian caveat, the Xanadu SPAC contrast) and **§07 "Who owns the market"** (direct household ownership 16.5% vs. 45.5% look-through, StatCan NBSA). §03's IPO-drought beat also gained the take-private off-ramp (~C$125B left the TSX via privatisation/M&A since 2023).
- `financing-gap.html` — new **§01b "The broader comparator set"** (a second, clearly-labelled 15-jurisdiction VC/GDP chart on the Compendium §3.1 basis, alongside — not replacing — the existing 5-country OECD/Landscape chart), new **§02 "Fewer founders"** (BDC Index of New Entrepreneurial Activity, 3.0→1.3 per 1,000 working-age Canadians 2000–2022, −57%, with StatCan self-employment-share corroboration), and new **§06 "The capital sitting idle next door"** (≈C$1.02T of unused TFSA room nationally, ≈C$411B in Ontario).
- `strengths.html` — new **band 06 "A second cluster · Ottawa & Kanata North"** (C$17.9B output / C$9.7B GDP / ~31k direct jobs / 800+ firms; CBRE tech-workforce-density comparison to the Bay Area; the DND/CSE/DRDC anchor; the Nortel-lineage thread into Ciena's Kanata R&D presence).
- `growth-prize.html` — new **§06b** giant-statement beat (Leaders Fund, Sept 2025: 32.4% of 2024-cohort Canadian-led high-potential startups HQ'd in Canada, down from 67%+ in 2015–19) and a household stock/flow mininote at §01 tying the 2.76:1 housing-vs-IP investment ratio to the real-estate-heavy household balance sheet (47.5%/51.2% Canada/Ontario).
- `exchanges.html` — new **§05 interlisting module** (KPI row: 206/30.7%, ~85% US, +7 net vs. Feb, 491 TSXV-on-OTC; an 11-sector interlisting-rate chart, Mining highest at 53.6%) plus a TSXV→TSX graduation-pipeline stat folded into §04 (222 of 668 Feb-2026 TSX issuers were TSXV graduates) and a CSE liquidity-concentration enrichment.
- `global-exchanges.html` — new on-page basis note clarifying WFE's "listed companies" indicator is an all-in count (TMX 2025 = 3,701, reconciling with the vault's own MiG all-in tally of 3,744) and is **not** comparable to the site's 672 operating-issuer figure used elsewhere.
- `champions.html` — five roster rows (Wealthsimple, StackAdapt, Clio, Kinaxis, MDA Space) wired to new case pages, completing the roster (15/15 + Nortel); a new §03 through-line tying the "missing middle" (C$2–10B TSX tier) to the roster's at-risk/in-play cluster (Cohere, Waabi, Xanadu, Tenstorrent).
- `methodology.html` — a new **eighth data layer** ("StatCan national accounts & household series" — NBSA, Survey of Financial Security, RRSP, Labour Force Survey self-employment, CRA TFSA stats) and new caveat rows for the interlisting cut, the sector-cut partiality (5 of 9 growth sectors map 1:1 to a TMX category, 2 are † partial proxies, 3 have none), the formation-decline no-international-comparator ruling, and the WFE all-in basis.

**5 new case pages** (`cases/wealthsimple.html`, `stackadapt.html`, `clio.html`, `kinaxis.html`, `mda-space.html`) built strictly to the existing template (`cases/README-cases.md` §§1–8) from adequate existing vault studies — no new vault research was required. `cases/` now holds **19 pages** (16 domestic incl. Nortel, 3 international); the champions.html roster is **fully linked, 15/15 + Nortel = 16/16**. The outcome-tally SVG on champions.html was unchanged — it already counted all 15 by outcome before the 5 pages existed to link to.

**New `window.PROV` keys added:** `tmxsec` (growth-sectors §05), `interlist` (public-markets §06 / exchanges §05, shared key), `ownership` (public-markets §07), `grad` (exchanges, graduation pipeline), `vcgdpwide` (financing-gap §01b), `formation` (financing-gap §02), `household` (financing-gap §06), `ottawa` (strengths band 06), `flight` (growth-prize §06b), `stakes` (champions §03), `basis` (global-exchanges, WFE reconciliation). Every page's PROV↔pill bijection was verified after these additions (per-page scan reports) and again after the cohesion-pass fixes.

**Convention notes from tonight:**
- **De-duplication ruling — the "US exit ramp."** Both `public-markets.html` §06 and `exchanges.html` §05 originally printed the full NYSE/Nasdaq/NYSE-Amer venue split under the same title. Resolved per the site's "one full treatment + pointers" rule: **`exchanges.html` owns the granular venue-by-venue and sector-by-sector interlisting cut** (retitled "Interlisting, mapped"); **`public-markets.html` owns the MJDS/cross-listing-premium narrative analysis** and now carries a summary stat + a pointer to the Exchanges page for the detailed cut.
- **Chart hierarchy (§5.5a).** One subject per chart is enforced — the exchanges.html interlisting-rate chart's Mining bar is the sole subject; a Technology "biggest gainer" annotation that had borrowed subject styling was demoted to a plain ramped bar with a dim annotation.
- **VC/GDP bases remain unmixed.** The new §01b comparator chart uses the Compendium §3.1 basis (15 jurisdictions, Canada 0.35%, mid-pack) explicitly labelled separately from the existing OECD/Landscape basis (5 jurisdictions, Canada 0.20%) that the page's dollar-gap arithmetic is built on — the two are never averaged, per `methodology.html`'s existing dual-basis ruling. The same rebase was applied to a stale, unsourced pair of figures on `what-works.html`'s VC/GDP tile grid (France and South Korea had both been miskeyed to 0.25%; corrected to 0.14% and 0.50% respectively on the Compendium basis).
- **Map visualization excluded, deliberately.** No geography/map visual was added anywhere on the site tonight. The vault's own history (see `what-works.html`'s inline "MAP DECISION" comment and `CLAUDE.md`'s July 6 "critical lesson") already replaced a D3+TopoJSON world map with the build-time inline-SVG comparator tile grid, because Chart.js/D3-CDN maps never rendered on Carlo's machine and no working inline-SVG geography asset exists. That decision stands; it was not revisited or reversed.

**QA:** the D4 cohesion-fixer pass corrected four HIGH figure-consistency issues surfaced by the audit (all confined to `public-markets.html`'s "three boards" cards, which had been carrying stale Feb-2026 vintage figures — C$6.8T→C$5.96T, C$164B→C$142.5B, Topicus C$7.8B→C$8.3B, CPC shells 165→161), the exit-ramp de-duplication above, the Wealthsimple/Beacon deal-figure misattribution on `growth-sectors.html`, an accessibility fix (a board-toggle aria-label that wasn't updating on `growth-sectors.html`), and the §5.5a chart-hierarchy fix. A scripted QA pass (`qa_site.py`) then confirmed all 32 site files clean (external scripts, storage APIs, literal hex in SVG, gradient-reference bijection, PROV bijection, tag balance, internal links/anchors) and every inline `<script>` block parses with `node --check`.

### Content additions (Session 2 — daytime, July 12 2026)

A same-day follow-on session executed three of Carlo's asks: site-wide mobile navigation (see "Mobile nav pattern" above), the Atlas (`atlas.html`), and the Canada & Ontario Profile page (`canada-ontario.html`). A fresh-eyes review pass (`research/review-new-pages.md`) verified both new pages against canon — 45+ figures sampled, 9 initiatives spot-checked, PROV bijection and tag balance clean — with **0 fixes applied** and 3 report-only flags (see `CLAUDE.md`'s July 12 daytime entry for the flags themselves). A scripted QA pass then confirmed all **34** site files clean.

**The Atlas data pipeline (`atlas.html`).** The map is a **build-time inline SVG** (no runtime map library, no external tiles) — the same zero-external-scripts discipline as every other chart on the site:
- **Map asset.** Built from **Natural Earth 110m** (public domain) via a Python/geopandas pipeline, projected in `natearth` (an equal-area-ish world projection chosen to keep both a legible world view and a legible Europe inset). The build deliberately **splits out Greenland, French Guiana and Svalbard** from their parent-country highlight fills (they are not comparator jurisdictions and would otherwise mislead a reader scanning the highlighted set) and renders **Singapore as a marker only** (too small to fill legibly at world scale). The **Europe inset** (`#europemap`, paired with `#worldmap` in `.mapgrid`) solves the historical crowding problem noted in Carlo's own recollection ("we ended with a Europe zoom-in to fix a scale issue") — see `CLAUDE.md`'s July 12 daytime entry for the map-decision reversal framing.
- **Source scripts.** The map-generation and data-assembly scripts (`map-asset/` and the `atlas-data` build) are **session-workspace artifacts, not vault files** — they live outside `/home/claude/vault` in the build session that produced this page, the same way `research/build_cut.py` (the TMX sector-cut script referenced in the Planning review page) is a session artifact, not a vault file. **To regenerate the map or the data**, re-run that pipeline in a fresh session: (1) re-derive jurisdiction geometries from Natural Earth 110m with the same Greenland/French-Guiana/Svalbard split and Singapore-marker-only rule, (2) rebuild the per-jurisdiction metrics + initiatives JSON from the Compendium (`Reference/International Comparator Data Compendium.md`) and the International Case Studies (`Themes/International Case Studies/`), cross-checked against `comparators.html` §03 and `what-works.html` §04/§04b for the 7 core dimensions, (3) re-embed both as inline SVG / `window.ATLAS` data at build time — there is no live/runtime fetch. **170 metrics + 71 initiatives** across the 19 packages in the current build; jurisdiction click-throughs use real SVG `<a>` anchors (`.jlink`), so they work with JS entirely off.
- **Coverage & the Italy call.** 19 jurisdictions: Canada (subject) + 14 core comparators + 4 expansion comparators (†, honestly thin, ⚠️-flagged per Compendium). **Italy is deliberately not a 20th jurisdiction package** — it's not one of the site's comparator jurisdictions (confirmed against the `comparators.html` chip picker) — but its transferable content (the STAR Segment liquidity mechanism, the PIR cautionary lesson) is honoured as a lesson-source, already surfaced on `what-works.html`'s program toolkit. Full coverage table, conflict log and the per-jurisdiction rationale live in the session's `research/atlas-data/NOTES.md` (not a vault file).
- **Accent:** violet (comparator/international family — see the accent table above).

**Canada & Ontario Profile ownership rulings (`canada-ontario.html`).** Per the site's "one page owns the full treatment, other pages point to it" convention, this page **owns outright**:
- The **household & investor base** — the fullest on-site treatment of accredited-investor bounded estimates, TFSA "maxed-out" rate (8.9%/8.6%), RRSP contributor counts, and the Ontario/Canada household net-worth breakout (median C$665,600 Ontario / C$519,700 Canada) — a materially fuller cut than `public-markets.html` §07's direct/look-through ownership figures or `financing-gap.html` §06's TFSA-room figure, which this page cross-links rather than restates.
- The **consolidated policy-scaffolding initiatives inventory** (§07) — federal/provincial × capital-side/innovation-side, typed with a `.sidetag` chip (accent = capital-side, grey = innovation-side): VCAP (C$390M→C$1.4B, 3.7:1), VCCI 2017 (C$372M→C$1.9B, 5.1:1) + VCCI 2021 (C$450M) + VCCI 4.0 (C$1B), BDC's 62% LP share of capital actively invested in Canadian VC, SR&ED, Flow-Through Shares, the Capital Pool Company mechanism, Invest Ontario, AI for All, the OSC Strategic Plan 2024–2030's Goal-5 capital-formation mandate, and others. **No other page owns a consolidated inventory of enacted programs** — this is genuinely new content for the site, not a restatement.
- It **points elsewhere** for everything else: sectors (§03, one line each) → `growth-sectors.html`; capital markets scale (§05) → `public-markets.html`/`exchanges.html`; champions (§06, teaser only) → `champions.html`; the prize/stakes (§08, teaser only) → `growth-prize.html`; the endowment-vs-conversion tension (§09, short bridge) → `strengths.html` §07–08, which already carries that turn in full.
- Uses the **freshest canonical figures** where the vault has since superseded the primary source (`Reference/Canada & Ontario — Economic & Investor Profile.md`): Ontario nominal GDP **C$1,246.7B (2025 full year, Ontario Ministry of Finance OEA)**, not the primary source's own C$1.197T (2024) headline; housing-vs-IP ratio 2.76:1 (2025), not 3.0:1 (2024). See `Planning/Champions Report — Overnight Build Review — July 12 2026.md`'s Session 2 addendum for the full staleness-flag list (`research/profile-brief.md` §5, a session artifact).
- **Accent:** azure (report-spine family — this is a grounding/backbone page, not a comparator page).

### Publication note

`Champions-Report.html` in this folder (the 108KB single-file version) is **SUPERSEDED** by this multi-page shell — **do not edit it**. It is excluded from the repo via `.gitignore` (kept only as a local provenance artifact; it contains stale figures and the old Chart.js/D3 CDN dependencies, and must not be published).

A research synthesis / proof-of-concept, not a published report. Figures are point-in-time (TSX/TSXV May 2026; CSE end-2025; OECD VC/GDP 2024) and graded by confidence on the Methodology page; private-company valuations are last-priced/press-reported.
